@{
    ViewData["Title"] = "JoJo RPG";
}

<main class="home-page" style="max-width:480px;margin:2rem auto;padding:1rem;">
    <h1>JoJo RPG</h1>
    @if (TempData["Error"] is string error)
    {
        <p class="error" style="color:#c44;">@error</p>
    }

    <section>
        <h2>Create campaign (GM)</h2>
        <form method="post" action="/rooms">
            <label>Campaign name <input type="text" name="name" placeholder="Campaign" /></label>
            <button type="submit">Create room</button>
        </form>
    </section>

    <section style="margin-top:2rem;">
        <h2>Join as player</h2>
        <form method="post" action="/join">
            <label>
                Room code
                <input type="text" name="roomCode" required maxlength="16" autocomplete="off" placeholder="e.g. ABC12345" style="text-transform:uppercase" />
            </label>
            <label>
                Display name
                <input type="text" name="displayName" required maxlength="128" placeholder="Your name" />
            </label>
            <label>
                Player code <span style="font-weight:normal;color:#888">(optional — rejoin an existing character)</span>
                <input type="text" name="playerCode" maxlength="16" autocomplete="off" placeholder="Leave blank for a new character" style="text-transform:uppercase" />
            </label>
            <button type="submit">Join</button>
        </form>
        <p style="font-size:0.9rem;color:#888;margin-top:0.75rem;">Your player code is saved in this browser after your first join. Enter it manually to rejoin from another device.</p>
    </section>

    <form method="post" action="/session/leave" style="margin-top:2rem;">
        <button type="submit">Leave session</button>
    </form>
</main>
